<form method="post" action="{{ path('app_locatie_delete', {'id': locatie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ locatie.id) }}">
    <button class="btn">Delete</button>
</form>
